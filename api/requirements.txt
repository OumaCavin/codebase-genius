# Core API framework
fastapi==0.115.0
uvicorn==0.30.1

# Optional (useful for JSON handling, HTTP requests, etc.)
requests==2.32.3
pydantic==2.7.1

# If you’re calling external APIs or using Git-based logic
GitPython==3.1.45

# Optional (only if you actually use these in your routes)
pandas==2.3.3
numpy==2.3.4

# CORS support (FastAPI dependency)
python-multipart==0.0.9
